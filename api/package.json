{
  "name": "@rainmaker/api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.3.4",
    "hono": "^4.7.4",
    "zod": "^3.24.2"
  }
}